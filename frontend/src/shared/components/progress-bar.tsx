export function ProgressBar({ value }: { value: number }) { const color = value === 100 ? "bg-emerald-600" : "bg-blue-600"; return <div className="flex min-w-28 items-center gap-2"><div className="h-2 flex-1 overflow-hidden rounded-full bg-slate-200"><div className={`h-full rounded-full ${color}`} style={{ width: `${value}%` }} /></div><span className="text-xs font-semibold">{value}%</span></div>; }
