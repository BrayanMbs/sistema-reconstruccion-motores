import { InventoryShell } from "@/modules/auth/components/inventory-shell";
export default function InventoryLayout({ children }: { children: React.ReactNode }) { return <InventoryShell>{children}</InventoryShell>; }
