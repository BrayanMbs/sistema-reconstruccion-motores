"use client";

import { Icon } from "@/shared/components/icon";
import type { PublicOrderTracking, PublicTrackingMilestone } from "../models/public-tracking";

const statusLabels = { PENDING: "Pendiente de iniciar", IN_PROGRESS: "En proceso de taller", COMPLETED: "Reparación finalizada", CANCELLED: "Orden cancelada" } as const;
const statusStyles = { PENDING: "border-amber-200 bg-amber-50 text-amber-800", IN_PROGRESS: "border-blue-200 bg-blue-50 text-blue-800", COMPLETED: "border-emerald-200 bg-emerald-50 text-emerald-800", CANCELLED: "border-slate-200 bg-slate-100 text-slate-700" } as const;

export function PublicTrackingResult({ tracking, onAnotherQuery }: { tracking: PublicOrderTracking; onAnotherQuery: () => void }) {
  const status = statusLabels[tracking.status];
  return <div className="public-tracking-print mx-auto max-w-7xl px-4 py-8 sm:px-6 sm:py-12 lg:px-10"><section className="rounded-xl border border-[#cbd5e1] bg-white p-6 shadow-sm sm:p-8"><div className="flex flex-col justify-between gap-5 border-b border-slate-200 pb-6 sm:flex-row sm:items-start"><div><p className="text-xs font-bold uppercase tracking-wider text-[#475569]">Orden de trabajo</p><h1 className="mt-1 text-3xl font-bold tracking-tight">{tracking.orderNumber}</h1><p className="mt-2 text-sm text-[#475569]">{tracking.engineSummary} · {tracking.serviceType}</p></div><span className={`inline-flex w-fit rounded-full border px-3 py-1.5 text-sm font-semibold ${statusStyles[tracking.status]}`}>{status}</span></div>
    <div className="mt-6 grid gap-6 lg:grid-cols-[1fr_340px]"><div className="space-y-5"><section className="rounded-xl border border-[#cbd5e1] bg-[#f8fafc] p-5"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-wider text-[#475569]">Avance de la reparación</p><strong className="mt-2 block text-4xl tracking-tight text-[#0f172a]">{tracking.progress}%</strong></div><span className="text-sm font-semibold text-[#475569]">{status}</span></div><div className="mt-5 h-3 overflow-hidden rounded-full bg-slate-200"><div className="h-full rounded-full bg-[#2563eb] transition-all" style={{ width: `${tracking.progress}%` }} /></div><p className="mt-3 text-sm leading-relaxed text-[#475569]">{tracking.serviceDescription ?? "La orden se encuentra en seguimiento técnico. Te notificaremos cualquier actualización relevante."}</p></section>
        <PublicTimeline milestones={tracking.timeline} />
        <section className="rounded-xl border border-[#cbd5e1] bg-white p-5"><h2 className="text-sm font-bold uppercase tracking-wider text-[#475569]">Resumen de la orden</h2><dl className="mt-4 grid gap-4 sm:grid-cols-2"><Detail label="Servicio" value={tracking.serviceType} /><Detail label="Motor" value={tracking.engineSummary} /><Detail label="Fecha de ingreso" value={formatDate(tracking.receivedAt)} /><Detail label="Última actualización" value={formatDateTime(tracking.lastUpdatedAt)} /></dl></section>
      </div><aside className="space-y-5"><section className="rounded-xl border border-[#cbd5e1] bg-white p-5"><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-[#475569]"><Icon name="tag" className="size-4 text-[#2563eb]" />Entrega estimada</div><p className="mt-3 text-xl font-bold">{tracking.estimatedDate ? formatDate(tracking.estimatedDate) : "Por confirmar"}</p><p className="mt-3 border-t border-slate-100 pt-3 text-xs leading-relaxed text-[#475569]">La fecha es estimada y puede variar según las pruebas técnicas y disponibilidad de piezas.</p></section>
        <section className="rounded-xl border border-[#cbd5e1] bg-slate-50 p-5"><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-[#16324f]"><Icon name="shield" className="size-4" />Privacidad y seguridad</div><p className="mt-3 text-xs leading-relaxed text-[#475569]">Este portal muestra únicamente información técnica autorizada. No publica saldos, pagos, inventario, datos del personal ni notas internas.</p></section>
        <section id="ayuda" className="rounded-xl border border-[#cbd5e1] bg-white p-5"><h2 className="text-sm font-bold">¿Tienes dudas sobre esta orden?</h2><p className="mt-2 text-xs leading-relaxed text-[#475569]">Comunícate directamente con el taller para consultas de pagos, facturación o retiro.</p><a href="tel:" className="mt-4 flex h-10 items-center justify-center gap-2 rounded-lg border border-[#cbd5e1] bg-[#f8fafc] text-xs font-semibold text-[#16324f] transition hover:bg-slate-100"><Icon name="phone" className="size-4" />Contactar a la empresa</a></section>
      </aside></div>
  </section><div className="mt-5 flex flex-col justify-between gap-3 rounded-xl border border-[#cbd5e1] bg-white p-4 shadow-sm sm:flex-row sm:items-center"><p className="text-xs text-[#475569]">Finaliza la consulta cuando termines para proteger tu código de seguimiento.</p><div className="flex gap-3"><button onClick={() => window.print()} className="rounded-lg border border-[#cbd5e1] bg-[#f8fafc] px-4 py-2.5 text-xs font-semibold text-[#16324f] hover:bg-slate-100">Imprimir</button><button onClick={onAnotherQuery} className="flex items-center gap-2 rounded-lg bg-[#2563eb] px-4 py-2.5 text-xs font-semibold text-white hover:bg-[#1d4ed8]"><Icon name="search" className="size-4" />Otra consulta</button></div></div>
  </div>;
}

function Detail({ label, value }: { label: string; value: string }) { return <div><dt className="text-xs font-semibold uppercase tracking-wider text-[#475569]">{label}</dt><dd className="mt-1 text-sm font-semibold text-[#0f172a]">{value}</dd></div>; }
function PublicTimeline({ milestones }: { milestones: PublicTrackingMilestone[] }) { return <section className="rounded-xl border border-[#cbd5e1] bg-white p-5"><div className="flex items-center justify-between gap-3"><div><h2 className="text-sm font-bold uppercase tracking-wider text-[#475569]">Historial de avance</h2><p className="mt-1 text-xs text-[#475569]">Actualizaciones técnicas autorizadas de tu reparación.</p></div><span className="rounded-full bg-blue-50 px-2.5 py-1 text-xs font-semibold text-[#2563eb]">{milestones.length} hitos</span></div><ol className="mt-5 space-y-5 border-l-2 border-slate-200 pl-5">{milestones.map((milestone, index) => <li key={`${milestone.type}-${milestone.occurredAt}-${index}`} className="relative"><span className={`absolute -left-[1.73rem] top-0.5 size-3 rounded-full border-[3px] border-white ${index === milestones.length - 1 ? "bg-[#2563eb] ring-2 ring-blue-100" : "bg-slate-400"}`} /><div className="flex flex-col justify-between gap-1 sm:flex-row sm:gap-4"><div><h3 className="text-sm font-semibold text-[#0f172a]">{milestone.title}</h3><p className="mt-0.5 text-xs leading-relaxed text-[#475569]">{milestone.message}</p></div><div className="shrink-0 text-left sm:text-right"><span className="text-xs font-semibold text-[#475569]">{milestone.progress === null ? "" : `Avance ${milestone.progress}%`}</span><time className="mt-0.5 block text-[11px] text-slate-500">{formatDateTime(milestone.occurredAt)}</time></div></div></li>)}</ol></section>; }
const formatDate = (value: string) => new Intl.DateTimeFormat("es-GT", { dateStyle: "long" }).format(new Date(value));
const formatDateTime = (value: string) => new Intl.DateTimeFormat("es-GT", { dateStyle: "medium", timeStyle: "short" }).format(new Date(value));
